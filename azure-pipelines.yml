trigger:
- master

pool:
  name: Default
jobs:
- job: one_job  
  steps:
  - script: |
      echo Add other tasks to build, test, and deploy your project.
    displayName: 'Run a multi-line script'

resources:
  repositories:
    - repository: self
      trigger: none
